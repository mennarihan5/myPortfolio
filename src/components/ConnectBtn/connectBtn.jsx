import styles from './styles.module.scss';

const ConnectBtn = () => {
  return (
    <button className={styles['connect-btn']}>
        Connect with me!
    </button>
  )
}

export default ConnectBtn;